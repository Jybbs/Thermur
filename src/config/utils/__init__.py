"""
Configuration utilities for hydra-zen integration.

This package provides centralized utilities for building configurations with
hydra-zen, ensuring consistency across all configuration domains. The primary
exports are enhanced versions of hydra-zen's store and build functions that
apply project-wide defaults.

Key features:
- Centralized hydra-zen imports to avoid version conflicts
- Standard build function with automatic signature population
- Extensible foundation for future configuration enhancements
"""